/*
 * Application JavaScript.
 *
 * Livewire ships its own script (injected by @livewireScripts in the layout)
 * and brings Alpine with it, so almost nothing is needed here. Prefer
 * Livewire actions over hand-written listeners -- it keeps state in one place.
 */
